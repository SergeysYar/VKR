# Publication Scripts

This folder contains scripts that generate publication-oriented artifacts:

- `generate_publication_layouts.py`
- `generate_publication_scene_set.py`
- `generate_single_scene_multiview_dataset.py`

Legacy entrypoints are still available in `scripts/` as thin wrappers for backward compatibility.
